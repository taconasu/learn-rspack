{
  "name": "rspack-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "styled-components": "^6.0.0-rc.5",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "dev": "rspack serve -c rspack.config.ts",
    "analyze": "rspack build --analyze -c rspack.config.ts",
    "build": "NODE_ENV=production rspack build -c rspack.config.ts",
    "build-windows": "set NODE_ENV=production&& rspack build -c rspack.config.ts",
    "config": "rspack serve",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@rspack/cli": "^0.2.3",
    "@svgr/webpack": "^8.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
